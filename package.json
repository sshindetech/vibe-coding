{
  "name": "vibe-coding",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend/gateway",
    "backend/ai-service",
    "backend/excel-service"
  ],
  "scripts": {
    "frontend": "npm run start --workspace frontend",
    "gateway": "npm run start --workspace backend/gateway",
    "gateway:dev": "npm run start:dev --workspace backend/gateway",
    "ai-service": "npm run start --workspace backend/ai-service",
    "ai-service:dev": "npm run start:dev --workspace backend/ai-service",
    "excel-service": "npm run start --workspace backend/excel-service",
    "excel-service:dev": "npm run start:dev --workspace backend/excel-service"
  },
  "dependencies": {
    
  },
  "devDependencies": {
    "@types/multer": "^1.4.13"
  }
}
